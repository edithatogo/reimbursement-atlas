# Specification

Define a versioned JSON Schema vocabulary shared with `global-medicines-atlas`
and `archive-govt-nz` without adding a shared runtime dependency. Conformance
must cover layer names, SHA-256 identity, lineage, rights decisions, promotion
status, adjacent transitions, complete gates, and fail-closed behavior.
