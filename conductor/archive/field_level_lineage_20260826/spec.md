# Specification

Record source field, transformation identity, output field, code version,
input/output checksums, and rights decision for governed transformations.
Export deterministic native JSONL, PROV-O, RO-Crate, and OpenLineage views.
Version the shared interoperability schema without modifying medallion v1.
