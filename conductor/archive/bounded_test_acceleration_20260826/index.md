# Bounded local test acceleration profiles

Status: completed

The repository provides opt-in fast, changed-code, benchmark, and `sysmon` profiles. They are local feedback aids only: authoritative CI continues to execute the complete suite with coverage, mutation, security, deterministic-generation, rights, and release gates unchanged.
