# Parallel CI and bounded test execution
Status: completed

CI uses bounded test parallelism and cancels superseded heavy runs while retaining full coverage, browser, security, and deterministic-generation gates.
