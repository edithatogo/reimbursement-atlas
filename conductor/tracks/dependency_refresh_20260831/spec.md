# Specification

Review and integrate compatible dashboard, Python and GitHub Actions updates
one PR at a time. Regenerate supply-chain and downstream evidence from the
reviewed lockfiles and pinned action identities, then require local quality,
deterministic generation and exact-head hosted checks before protected merge.

Reconcile remaining #797 drift after these merges. TypeScript 7 must remain an
explicit #362 external compatibility watch until supported checker peers and
typed tests pass. Do not weaken gates, fabricate approvals, publish new records
or submit papers/preprints. Standing approval scope applies to routine renewal.

Verify merged trees before removing branches. Archive only after bounded
maintenance acceptance is satisfied and genuine external gates are recorded.
